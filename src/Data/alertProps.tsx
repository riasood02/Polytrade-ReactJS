import { ToastOptions } from "react-toastify";

export const alertProperties: ToastOptions = {
  position: "top-right",
  autoClose: 3000,
  hideProgressBar: false,
  closeOnClick: true,
  draggable: false,
  progress: undefined,
  theme: "light",
};
